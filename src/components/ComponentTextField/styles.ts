import theme, { Theme } from '@material-ui/core';
const styles: any = (theme: Theme) => ({
    resize: {
        fontSize: '1.6rem',
        paddingTop: '15px',
    },
    labelRoot: {
        fontSize: '1.6rem',
    },
});

export default styles;
